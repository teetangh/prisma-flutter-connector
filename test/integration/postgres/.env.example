# PostgreSQL connection URL
POSTGRES_DATABASE_URL="postgresql://test_user:test_password@localhost:5432/test_db?schema=public"

# GraphQL backend URL (after starting the backend)
GRAPHQL_ENDPOINT="http://localhost:4000/graphql"
